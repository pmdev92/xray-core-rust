namespace xray {
void start_xray_logger(boolean is_all_packages, AndroidLogger logger);
void start_xray_core(u32 id, string path, ProtectFd protector);
void init_asset_path(string path);
void shutdown_xray_core(u32 id);
};


callback interface ProtectFd {
boolean protect(u64 id);
};

callback interface AndroidLogger {
void log(string message);
};